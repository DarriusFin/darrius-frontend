// Main frontend logic
const API_BASE = "https://api.darrius.ai";

document.getElementById("subscribeBtn").addEventListener("click", () => {
  alert("Subscription flow will be connected to Stripe + API backend.");
});
